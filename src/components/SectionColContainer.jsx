function SectionColContainer({ children }) {
  return (
    <section className="flex flex-col items-center justify-center gap-8">
      {children}
    </section>
  );
}

export default SectionColContainer;
